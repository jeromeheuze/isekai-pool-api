# koto.isekai-pool.com — zny-nomp website (pool portal on 127.0.0.1:8080)
# Merge into sites-available or include from main nginx.conf.
# After HTTP works: certbot --nginx -d koto.isekai-pool.com

server {
    listen 80;
    listen [::]:80;
    server_name koto.isekai-pool.com;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
